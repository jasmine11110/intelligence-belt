<view class="container">
  <view class="header">
    <icon type="success" size="40" color="#09BB07"/>
    <text class="title">赟原科技蓝牙设备</text>
    <text class="subtitle">智能设备连接平台</text>
  </view>
  <view class="content">
    <button 
      class="login-btn" 
      bindtap="handleLogin" 
      disabled="{{isLoggingIn}}"
      loading="{{isLoggingIn}}"
    >
      {{isLoggingIn ? '登录中...' : '立即登录'}}
    </button>
    <view class="tips">
      <text>登录后将自动搜索附近的蓝牙设备</text>
    </view>
  </view>
</view>